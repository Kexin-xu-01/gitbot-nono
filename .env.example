# Non-secret configuration — copy to .env and fill in.
# API credentials (GITHUB_TOKEN, GEMINI_API_KEY) must NOT go here —
# under nono proxy mode, they are injected as phantom tokens.

WEBHOOK_SECRET=your_webhook_secret
GITHUB_REPO=owner/repo
DOCS_URL=
DEBUG=false
